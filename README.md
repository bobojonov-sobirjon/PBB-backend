# PBB Backend - Строительная компания API

Бэкенд система для строительной компании ПВВ Строй с полной админ-панелью и REST API.

## 🚀 Технологии

- **Python 3.13**
- **Django 6.0.2**
- **Django REST Framework 3.16.1**
- **PostgreSQL**
- **drf-spectacular** (Swagger/OpenAPI документация)
- **Pillow** (обработка изображений)

## 📋 Возможности

### 1. Категории услуг
- **Главные категории** (Эконом ремонт, Стандартный ремонт, Дизайнерский ремонт, Ландшафтное освещение)
- **Подкатегории** с фотогалереей работ
- Иерархическая структура категорий

### 2. Портфолио
- **Наши проекты** - галерея выполненных работ
- Возможность добавления неограниченного количества фото

### 3. Процесс работы
- **5 шагов работы** с описанием каждого этапа
- От подачи заявки до выполнения работ

### 4. Видео-галерея
- **YouTube интеграция**
- Счетчик просмотров
- API для увеличения просмотров

### 5. Преимущества компании
- **Почему выбирают нас** - 6 ключевых преимуществ
- Доступность, Профессионализм, Удобство, Гарантии, Внимательность, Ответственность

### 6. Отзывы клиентов
- Система отзывов с рейтингом (1-5 звезд)
- **Модерация отзывов** перед публикацией
- POST API для отправки отзывов

### 7. Заявки на обратный звонок
- Форма заказа обратного звонка
- Отслеживание статуса обработки в админ-панели

## 📡 API Endpoints

### GET (Получение данных)

```
GET /api/categories/                      - Все категории с подкатегориями
GET /api/service-details/?sub_category_id=1 - Фото услуг по подкатегории
GET /api/projects/                        - Галерея проектов
GET /api/work-steps/                      - 5 шагов работы
GET /api/youtube-videos/                  - YouTube видео
GET /api/why-choose-us/                   - Преимущества компании
GET /api/client-reviews/                  - Отзывы клиентов (активные)
```

### POST (Создание данных)

```
POST /api/youtube-videos/increment-views/?id=1  - Увеличить просмотры видео
POST /api/client-reviews/create/                - Оставить отзыв
POST /api/callback-request/                     - Заказать обратный звонок
```

## 🛠️ Установка и запуск

### 1. Клонировать репозиторий

```bash
git clone <repository-url>
cd PBBBackend
```

### 2. Создать виртуальное окружение

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
```

### 3. Установить зависимости

```bash
pip install -r requirements.txt
```

### 4. Настроить базу данных

Создать базу данных PostgreSQL:

```sql
CREATE DATABASE pbb;
CREATE USER postgres WITH PASSWORD '0576';
GRANT ALL PRIVILEGES ON DATABASE pbb TO postgres;
```

Обновить настройки в `config/settings.py`:

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'pbb',
        'USER': 'postgres',
        'PASSWORD': 'your_password',
        'HOST': 'localhost',
        'PORT': 5432,
    }
}
```

### 5. Применить миграции

```bash
python manage.py migrate
```

### 6. Создать суперпользователя

```bash
python manage.py createsuperuser
```

### 7. Создать тестовые данные (опционально)

```bash
python manage.py create_fake_data
```

Это создаст:
- 4 главные категории
- 16 подкатегорий
- 5 шагов работы
- 4 YouTube видео
- 6 преимуществ компании
- 4 отзыва клиентов

### 8. Запустить сервер

```bash
python manage.py runserver
```

Сервер будет доступен по адресу: `http://localhost:8000`

## 📚 Документация API

После запуска сервера доступна автоматическая документация:

- **Swagger UI**: http://localhost:8000/swagger/
- **ReDoc**: http://localhost:8000/redoc/
- **OpenAPI Schema**: http://localhost:8000/schema/

## 🔐 Админ-панель

Админ-панель доступна по адресу: http://localhost:8000/admin/

### Возможности админ-панели:

1. **Управление категориями**
   - Главные категории
   - Подкатегории
   - Фотогалереи услуг (inline)

2. **Управление контентом**
   - Наши проекты
   - Шаги работы
   - YouTube видео
   - Преимущества компании

3. **Модерация**
   - Отзывы клиентов (активация/деактивация)
   - Заявки на обратный звонок (отметка обработанных)

4. **Превью изображений**
   - Все изображения показываются с превью
   - В списках и в формах редактирования

## 📦 Структура проекта

```
PBBBackend/
├── apps/
│   └── website_config/        # Основное приложение
│       ├── models.py          # Модели данных
│       ├── admin.py           # Настройки админ-панели
│       ├── views.py           # API views
│       ├── serializers.py     # DRF сериализаторы
│       ├── urls.py            # URL маршруты
│       └── management/
│           └── commands/
│               └── create_fake_data.py  # Команда для создания тестовых данных
├── config/
│   ├── settings.py            # Настройки Django
│   ├── urls.py                # Главные URL
│   └── middleware/            # Middleware
├── requirements.txt           # Зависимости Python
└── manage.py                  # Django CLI
```

## 📊 Модели данных

### Category (Категория)
- Иерархическая структура (parent-child)
- MainCategory (proxy) - главные категории
- SubCategory (proxy) - подкатегории

### ServiceDetails (Детали услуг)
- Фотографии для каждой подкатегории
- Сортировка по order

### OurProject (Наши проекты)
- Галерея выполненных проектов

### WorkStep (Шаги работы)
- 5 шагов процесса работы
- Номер шага (unique)

### YouTubeVideo (YouTube видео)
- Ссылка на YouTube
- Счетчик просмотров
- Thumbnail (опционально)

### WhyChooseUs (Почему выбирают нас)
- Преимущества компании
- Сортировка по order

### ClientReview (Отзывы клиентов)
- ФИО, комментарий, рейтинг
- Модерация (is_active)

### CallbackRequest (Заявки на звонок)
- Имя, телефон
- Статус обработки

## 🎨 API Примеры

### Получить категории с подкатегориями

```bash
curl http://localhost:8000/api/categories/
```

**Response:**
```json
{
  "count": 4,
  "results": [
    {
      "id": 1,
      "name": "Эконом ремонт",
      "is_active": true,
      "created_at": "2026-02-13T...",
      "sub_category_list": [
        {
          "id": 2,
          "name": "Поклейка обоев",
          "is_active": true,
          "service_details": [...]
        }
      ]
    }
  ]
}
```

### Оставить отзыв

```bash
curl -X POST http://localhost:8000/api/client-reviews/create/ \
  -H "Content-Type: application/json" \
  -d '{
    "full_name": "Иванов Иван",
    "comment": "Отличная работа!",
    "rating": 5
  }'
```

**Response:**
```json
{
  "success": true,
  "message": "Отзыв успешно отправлен и будет опубликован после модерации",
  "review": {
    "id": 5,
    "full_name": "Иванов Иван",
    "comment": "Отличная работа!",
    "rating": 5,
    "created_at": "2026-02-13T..."
  }
}
```

### Заказать обратный звонок

```bash
curl -X POST http://localhost:8000/api/callback-request/ \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Иван",
    "phone": "+998901234567"
  }'
```

**Response:**
```json
{
  "success": true,
  "message": "Заявка успешно отправлена! Мы свяжемся с вами в ближайшее время",
  "callback": {
    "id": 1,
    "name": "Иван",
    "phone": "+998901234567",
    "created_at": "2026-02-13T..."
  }
}
```

## 🔧 Настройки

### CORS (Cross-Origin Resource Sharing)

В `config/settings.py` настроены разрешенные источники:

```python
CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",
    "http://localhost:8000",
    "http://127.0.0.1:5173",
]
```

Добавьте свой frontend URL при необходимости.

### Медиа файлы

```python
MEDIA_URL = "/media/"
MEDIA_ROOT = os.path.join(BASE_DIR, "/var/www/media/")
```

Обновите `MEDIA_ROOT` для своего окружения.

## 📝 Команды управления

```bash
# Создать миграции
python manage.py makemigrations

# Применить миграции
python manage.py migrate

# Создать суперпользователя
python manage.py createsuperuser

# Создать тестовые данные
python manage.py create_fake_data

# Запустить сервер
python manage.py runserver

# Проверить систему
python manage.py check

# Собрать статические файлы
python manage.py collectstatic
```

## 🌐 Язык интерфейса

- **Админ-панель**: Русский
- **API**: JSON (мультиязычный)
- **Verbose names**: Русский

## 📄 Лицензия

Этот проект является собственностью компании ПВВ Строй.

## 👨‍💻 Разработка

Проект разработан с использованием современных практик Django и DRF:
- Class-based views
- Proxy models для разделения логики
- Inline admin для связанных объектов
- drf-spectacular для автодокументации
- Валидация данных на уровне сериализаторов
- Модерация пользовательского контента

## 📞 Поддержка

Для вопросов и поддержки обращайтесь к администратору системы.

---

**Mini Project** - Backend API для строительной компании | Django + DRF + PostgreSQL
